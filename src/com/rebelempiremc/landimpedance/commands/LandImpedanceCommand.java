package com.rebelempiremc.landimpedance.commands;

import org.bukkit.command.Command;
import org.bukkit.command.CommandExecutor;
import org.bukkit.command.CommandSender;

/*
** Main command for plugin, will only include commands like reload! Player commands will be in /land
 */
public class LandImpedanceCommand implements CommandExecutor {
    public boolean onCommand(CommandSender sender, Command cmd, String commandUsed, String[] args){
        return false;
    }
}
